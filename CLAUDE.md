# CLAUDE.md — Sokcho Insight (속초 인사이트)

> 이 문서는 본 저장소에서 작업하는 모든 Claude Code 세션이 가장 먼저 읽는
> **프로젝트 가이드(헌법)** 입니다. 컨셉·핵심 가치·기술 스택·MVP 범위·구조·
> 컨벤션을 정의하여 개발이 일관되게 진행되도록 합니다.

---

## 1. 프로젝트 개요

- **제품명:** Sokcho Insight (속초 인사이트)
- **컨셉:** **"광고 없는 진짜 속초를 만나다"**
  SNS 광고와 바이럴 마케팅으로 오염된 여행 정보 속에서, 현지인 가성비와
  공신력 있는 데이터를 추출해 제공하는 속초 여행 큐레이션 웹앱.
- **핵심 가치:** **데이터 무결성(Data Integrity)** — 인플루언서/협찬/광고성
  데이터를 배제하고 교차 검증된 신뢰 정보만 노출합니다.
- **타깃 사용자:**
  - 가성비와 신뢰할 수 있는 정보를 찾는 속초 여행객
  - 무장애(배리어프리) 동선이 필요한 유모차·휠체어 이용자 (추후 단계)
- **UI 언어:** 한국어

---

## 2. 핵심 가치 & 제품 원칙

1. **3단계 필터링 아키텍처**
   - 구글 평점(실용성) : 네이버 플레이스(인지도) = **5:5 비율 융합**
   - 블루리본 서베이 등 외부 검증 자료를 가중치로 반영
2. **광고/협찬 배제 원칙** — 광고성·협찬 콘텐츠는 노출하지 않습니다.
3. **광고 데이터 필터링(로드맵)** — 평점 표준편차 분석을 도입해 비정상적으로
   고평가된(인플루언서 추정) 데이터를 걸러내는 로직을 향후 추가합니다.
4. **신뢰 우선 UX** — 데이터 출처와 근거를 사용자에게 투명하게 표시합니다.

---

## 3. MVP 범위 (Phase 1) — *지금 집중할 것*

MVP는 아래 **2개 기능**에만 집중합니다.

1. **3단계 필터링 맛집 큐레이션**
   - 구글/네이버 평점을 5:5로 융합하고 검증 자료를 가중해 신뢰도 점수를 산출
   - 목업 데이터 기반으로 동작
2. **기상 연동 동선 큐레이션**
   - 우천·강풍 등 악천후 시 국립산악박물관·속초시립박물관 등
     실내 박물관 클러스터 위주의 대안 경로를 제안

### MVP 제외 (추후 단계)
- 스마트 주차 요금 계산기
- 배리어프리(무장애) 경로 추천
- 실시간 영업 상태 연동
- 사용자 인증 / 즐겨찾기 / 리뷰

---

## 4. 기술 스택

| 영역 | 선택 | 비고 |
| :--- | :--- | :--- |
| Frontend | **Next.js (App Router) + TypeScript + React** | SEO·컴포넌트 강점 |
| Styling | **Tailwind CSS** | 빠른 UI 구축 |
| Backend / DB | **Supabase (PostgreSQL)** | MVP는 목업 데이터, 점진적으로 Supabase 이행 |
| Auth | **없음 (MVP)** | 즐겨찾기/리뷰 도입 시 Clerk 검토 |
| 배포 | **Vercel** (제안) | Next.js 친화 |

---

## 5. 디렉터리 구조 (예정)

```
/app            # Next.js App Router 페이지/라우트
/components     # 재사용 UI 컴포넌트
/lib            # 데이터 접근, API 클라이언트
/lib/scoring    # 3단계 필터링 / 점수 융합 로직 (순수 함수)
/data           # 목업 데이터(JSON) — 맛집, 속초 8경, 박물관
/types          # 공용 타입 정의
```

---

## 6. 데이터 전략

- **Phase 1:** `/data/*.json` 목업(더미) 데이터로 시작 — 실제 API 키 불필요
- **데이터 모델 설계 원칙:** 향후 실제 API 스키마와 호환되도록 설계
  - 지도/장소: Google Places API, Naver Maps API
  - 날씨: OpenWeatherMap, 기상청 공공데이터
  - 관광 정보: TourAPI (한국관광공사)
  - 미식 검증: 블루리본 서베이 등
- **API 키 관리:** 실제 연동 시 `.env.local`로 주입하며 **절대 커밋 금지**

---

## 7. 코딩 컨벤션

- TypeScript **strict** 모드, 명시적 타입 사용
- React **함수형 컴포넌트** 사용, 컴포넌트는 작고 재사용 가능하게 분리
- 데이터 융합/점수 산출 로직은 `/lib/scoring`에 **순수 함수**로 분리하여
  테스트가 쉽도록 구성
- 외부 API 호출은 **서버 사이드**에서 수행해 키 노출을 방지

---

## 8. 개발 / 실행 명령어

> 스캐폴딩(Phase 2 시작) 후 확정 예정. 일반적인 Next.js 명령 기준:

```bash
npm run dev     # 개발 서버
npm run build   # 프로덕션 빌드
npm run lint    # 린트
```

---

## 9. 로드맵

- **Phase 1 (현재):** MVP 2개 기능 — 맛집 큐레이션 + 기상 연동 동선 (목업 데이터)
- **Phase 2:** Supabase 연동, 실제 API 통합, 스마트 주차 계산기 / 배리어프리 경로,
  실시간 영업 상태
- **Phase 3:** 평점 표준편차 기반 광고 데이터 필터링, OWASP Top 10 보안 점검,
  사용자 인증 도입

---

## 10. 보안 / 품질 원칙

- 시크릿·API 키는 **커밋 금지**, `.env.local` 사용
- 외부 API 호출은 서버 사이드에서만 수행 (클라이언트 키 노출 방지)
- 프로덕션 런칭 전 OWASP Top 10 취약점 진단 (Phase 3)
